/**
 * Naming Conventions
 * 
 * 1) 일반적으로 영어를 사용하며 문자와 숫자를 모두 사용 가능.
 * 2) 특수기호는 언더스코어와 달러를 사용 할 수 있다.
 * 3) 숫자로 이름을 시작 할 수 없다.
 *    ex) 1name 2good
 * 4) 키워드는 변수명으로 사용 할 수 없다.
 *  var var='var' 발발발~ 
 */
let myoungho = '명호';
var $ive = '아이브';
const _yuJin='안유진';

console.log(myoungho);
console.log($ive);
console.log(_yuJin);

/**
 * Naming convention
 * 
 * 1) camelCase - 대부분의 언어에서 많이 사용함.
 *  ex) appleManggo
 * 2) snake_case - 파이썬같은 언어에서 사용함.
 * 3) PascalCase - C# 마이크로소프트 계열의 언어에서 사용함.
 *    첫문자도 대문자로함
 */
const anYuJin = '안유진'; //camelCase
console.log(anYuJin);